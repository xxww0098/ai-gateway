[package]
name = "gw-oauth-hops"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "Shared OAuth hop planning: identity headers, cache pins, body rewrite. No sockets."

[lib]
doctest = false          # rule 2.10: internal crate, no external example readers

[lints]
workspace = true         # rule 4.2: the table is inert without this

[dependencies]
bytes.workspace = true
http.workspace = true
serde_json = { workspace = true, features = ["std"] }
